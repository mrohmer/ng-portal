import {PortalHostComponentStyleDirective} from './portal-host-component-style.directive';

describe('PortalHostComponentStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalHostComponentStyleDirective();
    expect(directive).toBeTruthy();
  });
});
