import { PortalHostDirective } from './portal-host.directive';

describe('PortalHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalHostDirective();
    expect(directive).toBeTruthy();
  });
});
