import { PortalSlotDirective } from './portal-slot.directive';

describe('PortalSlotDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalSlotDirective();
    expect(directive).toBeTruthy();
  });
});
